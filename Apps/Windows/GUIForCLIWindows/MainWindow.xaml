<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="GUIForCLIWindows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:GUIForCLIWindows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="GUI for CLI"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="AppTitleBar"
            Title="GUI for CLI"
            BackRequested="TitleBar_BackRequested"
            IsBackButtonVisible="{x:Bind NavFrame.CanGoBack, Mode=OneWay}"
            IsPaneToggleButtonVisible="True"
            PaneToggleRequested="TitleBar_PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView
            x:Name="NavView"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            OpenPaneLength="260"
            SelectionChanged="NavView_SelectionChanged">
            <NavigationView.PaneHeader>
                <StackPanel Margin="12,8,12,16" Spacing="6">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon
                            x:Name="BundlePaneIcon"
                            FontSize="22"
                            Glyph="&#xECAA;" />
                        <TextBlock
                            x:Name="BundlePaneTitle"
                            VerticalAlignment="Center"
                            FontWeight="SemiBold"
                            Text="GUI for CLI"
                            TextWrapping="Wrap" />
                    </StackPanel>
                    <TextBlock
                        x:Name="BundlePaneSummary"
                        Opacity="0.72"
                        TextWrapping="WrapWholeWords" />
                </StackPanel>
            </NavigationView.PaneHeader>
            <NavigationView.PaneFooter>
                <Border
                    x:Name="PaneResizeHandle"
                    Width="8"
                    Height="40"
                    HorizontalAlignment="Right"
                    Background="Transparent"
                    PointerMoved="PaneResizeHandle_PointerMoved"
                    PointerPressed="PaneResizeHandle_PointerPressed"
                    PointerReleased="PaneResizeHandle_PointerReleased"
                    ToolTipService.ToolTip="Drag to resize sidebar" />
            </NavigationView.PaneFooter>
            <Frame x:Name="NavFrame" />
        </NavigationView>
    </Grid>
</Window>
