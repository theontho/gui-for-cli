name: Windows native

on:
  pull_request:
    paths:
      - ".github/workflows/windows.yml"
      - "exp-platform/windows/dotnet/GUIForCLIWindows/**"
      - "exp-platform/go/gio/**"
      - "exp-platform/rust/dioxus-shell/**"
      - "exp-platform/windows/dotnet/GUIForCLIWindows.Core/**"
      - "exp-platform/windows/dotnet/GUIForCLIWindows.CoreTests/**"
      - "examples/**"
      - "platform/typescript/**"
      - "docs/schema/manifest.schema.json"
      - "Makefile"
      - "make.ps1"
      - "scripts/package-windows-gio.ps1"
  push:
    branches: [main]
    paths:
      - ".github/workflows/windows.yml"
      - "exp-platform/windows/dotnet/GUIForCLIWindows/**"
      - "exp-platform/go/gio/**"
      - "exp-platform/rust/dioxus-shell/**"
      - "exp-platform/windows/dotnet/GUIForCLIWindows.Core/**"
      - "exp-platform/windows/dotnet/GUIForCLIWindows.CoreTests/**"
      - "examples/**"
      - "platform/typescript/**"
      - "docs/schema/manifest.schema.json"
      - "Makefile"
      - "make.ps1"
      - "scripts/package-windows-gio.ps1"

permissions:
  contents: read

jobs:
  windows-native:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: platform/typescript/package-lock.json
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25.0"
      - name: Install WebUI dependencies
        run: npm ci
        working-directory: platform/typescript
      - name: Build Gio shell
        shell: pwsh
        run: |
          Set-Location exp-platform/go/gio
          go build ./...
      - name: Build Windows app
        shell: pwsh
        run: ./make.ps1 build
      - name: Build Dioxus shell
        shell: pwsh
        run: ./make.ps1 build-dioxus
      - name: Test Windows core
        shell: pwsh
        run: ./make.ps1 test-core
      - name: WebUI typecheck
        run: npm --prefix platform/typescript run check
      - name: WebUI tests
        run: npm --prefix platform/typescript test
      - name: Static Windows accessibility smoke
        shell: pwsh
        run: ./scripts/windows-ax-smoke.ps1 -StaticOnly
