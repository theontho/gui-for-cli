# Gemini Instructions

Follow `AGENTS.md`. This repository is a Swift template for iOS apps, macOS apps, and Swift CLIs.

Keep reusable code in `GUIForCLICore`, CLI code in `GUIForCLICLI`, and SwiftUI app code under `Apps/`.
